
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all read/write operations for prototype simplicity
      allow read, write: if true;
    }
  }
}
